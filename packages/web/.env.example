# Base URL of the optional jsona share server (server/ package).
# Leave empty for pure local-first usage (no server short links / workspace sync).
# When set, the web client enables "server short link" + GitHub workspace sync.
VITE_API_BASE=

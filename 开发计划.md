# jsona —— JSON 结构化文本处理工具 开发计划（v0.2）

> 定位参考：[Treease](https://treease.com/)（JSON / YAML / TOML / CSV Viewer with Graphs）
> 项目名：**jsona**（已定）
> 本文档为讨论底稿，决策已对齐，待细化实现。

---

## 1. 产品定位

一款**结构优先（structure-first）、本地优先（local-first）**的 JSON / YAML / TOML / CSV 可视化处理工具。核心不是"又一个代码编辑器"，而是：

- 把任意结构化文本解析成**可导航的树 / 图形**，让嵌套数据"看得见、追得到、改得动"。
- 源文本 ↔ 图形 ↔ 树路径 ↔ 输出预览 **锚定同一个文档状态**，任一侧修改同步刷新。
- **数据纯本地**：所有解析、编辑、可视化都在浏览器内完成，文档不上传任何服务器，从源头消除用户数据安全担忧（核心卖点）。
- 覆盖工作流：打开 → 看结构 → 追踪字段 → 塑形（格式化/压缩/排序键）→ 对比 → 转换 → 导出/分享。

与纯编辑器（VS Code）、纯查看器（JSON Hero）的差异：**双向编辑同步 + 图形上下文 + CLI 同引擎 + 本地零上传**。

---

## 2. 整体架构（已定：方案 A 变体）

### 2.1 主体：纯前端 Web App（方案 A）
- 浏览器内解析/渲染，**核心编辑器数据零后端、零上传**。
- 可静态托管（CDN / GitHub Pages / 自有域），隐私合规简单。

### 2.2 商业化所需的轻服务（仅增值能力）
数据本地与商业化**不冲突**。收费点中只有"分享链接 / 工作区同步"需要服务端，因此引入一个**极轻的独立分享服务（轻 Node 实现）**，职责单一：
- 接收已序列化的文档 → 存库 → 返回短链 → 按有效期过期；可选账号下支持工作区同步。
- 该服务**不接触用户其他数据**，仅存"被显式分享/主动保存的那份文档"，且可默认关闭（纯本地模式）。
- 核心编辑器、Graph、Compare、CLI 全部仍运行在用户本地。

### 2.3 数据隐私承诺（对外宣称）
- **"纯前端，数据不经过你的服务器"**——作为核心对外承诺。
- 仅当用户**主动**使用分享链接或登录后主动保存工作区时，数据才离开浏览器。

### 2.4 账号体系：可选账号（不强求）
- **匿名可用**：打开即用，所有数据在浏览器内存 / `localStorage`，零注册。
- **账号增值**：登录后可云端保存、**跨设备同步工作区**、管理自己的分享链接。
- **登录方式**：仅 **GitHub OAuth**（开发者最信任）+ 可选 Google。
- **承诺边界（重要）**：**不做"数据永久存储"承诺，只做"工作区同步"**。开发者不信任你把他的 JSON 存下来，但信任"我自己主动保存的"。对外话术强调"你主动保存的才同步"，而非"我们帮你存"。

> 结论：**方案 A 不影响商业化**。免费=本地全部功能；收费=云端增值（分享链接额度/有效期、工作区同步、AI 处理配额）。本地核心能力不收费。

### 2.3 测试策略调整
- **开发期走查/冒烟用 Chrome MCP**（连接本机 Chrome：导航、交互、`evaluate_script` 抽文本验证），取代 Playwright 安装与维护成本。
- Playwright 仅作为**可选 E2E 回归**保留（非必需依赖），不阻塞日常开发。

---

## 3. 技术栈（已定）

| 维度 | 选择 | 说明 |
|------|------|------|
| 框架 | React 19 + Vite + TypeScript（严格） | 纯前端 SPA，静态托管 |
| 解析 | `json5` / `yaml` / `toml` / `papaparse`(CSV) | 多格式统一为中间 AST |
| 图形视图 | `react-flow`（或自绘 SVG/canvas）+ `d3-force` 力导向 | Graph View 核心，布局可切换 |
| 树视图 | **自研虚拟滚动树**（必须，见 §6） | 超大文件不卡顿 |
| 编辑器 | `codemirror 6`（比 monaco 更轻、更适配纯前端） | 源码视图 + 语法高亮 |
| 状态 | Zustand | 管理"同一文档多视图"同步 |
| Diff | `diff`（文本）+ 自研结构化 diff | Compare |
| 测试 | Vitest（单测引擎）+ Chrome MCP（走查） | Playwright 可选 |
| CLI | `commander` + 复用同一解析引擎 | jq 风格查询/转换 |
| 分享服务 | 轻 Node（小后端，闭源） | 仅存分享文档+短链+工作区同步 |

---

## 4. 核心数据模型（中间 AST）

所有格式先归一化为统一节点树，再喂给各视图：

```
JsonNode = {
  id: string            // 稳定 id，用于视图间锚定/高亮
  path: string          // 如 .services.api.url
  key?: string          // 对象键 / 数组下标
  kind: 'object'|'array'|'string'|'number'|'boolean'|'null'
  value?: unknown
  children?: JsonNode[]
  start?: number        // 源文本偏移（用于高亮/追踪）
  end?: number
}
```

**关键设计**：每个节点携带 `path` 与源偏移区间，使 Graph ↔ Tree ↔ Source 三向高亮可对齐。

---

## 5. 功能模块规划（V1 目标已定）

### 5.1 核心（V1 必交付，全部本地免费）
1. **导入**：粘贴 / 上传文件 / 拖拽；支持 JSON/YAML/TOML/CSV。
2. **源码视图**：编辑器 + 语法高亮 + 行号。
3. **树视图**：折叠/展开、搜索节点、类型标注、值预览（**虚拟滚动**）。
4. **Graph View**：节点关系图，默认**力导向布局**（视觉冲击力强），提供**树形布局**切换（满足传统 JSON 树查看习惯）；点击节点高亮源文本与树路径。超大图按深度采样（见 §6.2）。
5. **双向同步**：源码编辑 → 实时解析重建树/图；树/图内编辑值 → 写回源码（原地更新，保留格式上下文）。
6. **塑形**：格式化 / 压缩(minify) / 按键名排序。
7. **Compare**：双文档结构化 diff；结构不可比时回退文本 diff。
8. **字段追踪**：从 Graph/Tree 搜索定位 → 源文本高亮 + 树路径揭示。
9. **日志提取**：从混合日志中隔离出活动 JSON 容器（JSONL / 内嵌 JSON）。
10. **超大文件**：≥256 KiB 显示解析进度；树/图**虚拟滚动 + 分块渲染**（见 §6）。
11. **导出**：转其他格式下载（JSON→YAML 等）。

### 5.2 增值（V1 目标，云端收费 / 配额制）
12. **CLI**：`jsona '.services.api.url' file.json`、`jsona -o yaml '.' file.json`、`jsona web ...` 启动可视化。与 Web 端**复用同一解析引擎**。
13. **分享链接**：序列化文档 → 短链（经轻分享服务），免费档有效期短（≤7天）/ 额度低，收费档有效期长（≤365天）/ 额度高。可默认关闭纯本地模式。

### 5.3 后续（V2+，AI 等）
14. **AI 处理**：自然语言查询/描述转换（配额制，需后端）。

---

## 6. 超大文件工程策略（提前做）

目标：即使数十 MB 的 JSON 也不卡死 UI。

- **解析分层**：流式/分块解析（CSV 用 papaparse 流式；JSON 用增量解析或 Web Worker 后台解析，主线程不阻塞）。
- **虚拟滚动**：树视图、Graph 节点列表均只渲染可视区域（树必做；Graph 大节点时做视口裁剪 + 聚合/折叠）。
- **Web Worker**：解析与 AST 构建放到 Worker，进度回传主线程显示进度条。
- **懒展开**：树默认只展开前 N 层，深层节点按需加载 children。
- **内存控制**：超大文档的图形视图提供"采样/聚合"模式（只渲染 top-N 分支），避免一次性建全图。

### 6.2 Graph View 布局与超大图采样（确定性，绝不随机）
- **默认布局：力导向（force-directed）**——更"数据可视化"、一眼看出数据热点与聚类。
- **切换布局：树形（tree）**——满足传统 JSON 树查看习惯。
- **采样策略（JSON 场景特别重要，必须确定性）**：
  - 节点 **> 500** 时：默认按**深度采样**（只展开前 3 层，更深层折叠为**聚合节点**），保留结构完整性。
  - 节点 **> 2000** 时：显示警告"当前数据量较大，建议使用表格视图"，并引导到树/表格视图。
  - **绝对禁止随机采样**——JSON 开发者会立刻发现"数据不对"，信任崩塌。所有采样必须可解释、可还原。

---

## 7. 商业模式：免费 / 收费差异化

### 7.1 原则
- **本地核心功能永久免费**，收费只针对**云端增值能力**（分享链接、工作区同步、AI），避免"核心功能收费"引发用户反感。
- **免费版不限制功能，只限制"量和时间"**——开发者会因量和时间自然升级，而非被功能卡住。
- 核心成本是**存储（分享链接的数据）**与**带宽（大文件传输）**，故收费按"文件大小 + 分享次数"切档。

### 7.2 定价档位（按文件大小 + 分享次数）

| 档位 | 价格 | 单文件上限 | 分享链接数 | 留存天数 | 其他 |
|------|------|-----------|-----------|---------|------|
| **免费** | ¥0 | 1 MB | 10 个 | 7 天 | 功能全开 |
| **Pro** | ¥29/月 或 ¥199/年 | 10 MB | 无限 | 30 天 | — |
| **Team** | ¥99/月 | 50 MB | 无限 | 90 天 + 密码保护 | — |

- **超大文件（>10MB）引导 CLI**：Web 端提示"建议使用 CLI 工具本地处理"（如 `jsona -i data.json -o graph.html`），正好形成 CLI 获客闭环。
- **本地超大文件处理本身免费**（本地算力不限），仅"通过 Web 分享/同步"受单文件上限约束。

### 7.3 开源策略（开源获客，闭源变现）

| 组件 | 协议 | 说明 |
|------|------|------|
| **CLI** | MIT | **必须开源，核心竞争力**：开发者可审计"数据是否被上传"、可离线 `jsona -i data.json -o graph.html`、可接入 CI/CD 自动生成数据结构文档、易社区传播（star/fork/博客）。 |
| **Web 可视化渲染引擎** | GPL | 开源保证衍生品也开源 |
| **分享服务后端（存储/统计）** | 闭源 | 变现点，不公开 |

> 这是"开源获客、闭源变现"模型：开发者用 CLI 用爽了，自然愿意付费买 Web 端的协作/分享能力。

### 7.4 差异化打法
- **分享协作**：生成 URL 给同事看，不用截图。
- **大文件处理**：浏览器 DevTools 打开 5MB JSON 会卡死，jsona 虚拟滚动/Worker 不卡。
- **图形化洞察**：力导向图一眼看出数据热点与聚类。

> 差异化定位：相比 Treease（图形/大文件/AI 在免费档均有月额度限制），jsona 把**本地全部核心能力（含 Graph、大文件）免费开放**，仅对"分享链接、工作区同步、AI"等云端能力收费，并以"数据纯本地、零上传 + CLI 开源"建立信任。

---

## 8. 阶段里程碑（已定）

| 阶段 | 目标 | 交付 |
|------|------|------|
| P0 架构对齐 | 方案 A 变体、技术栈、数据模型、商业化分层 | 本文档定稿 |
| P1 引擎 | 多格式解析 → 统一 AST + 源码偏移 + Worker 解析 | 解析库 + Vitest 单测 |
| P2 双视图+同步 | 源码视图 + 树视图（虚拟滚动）+ 双向同步 + 塑形/导出 | 可运行 Web App |
| P3 Graph+Compare | Graph View + Compare + 字段追踪 | 差异化能力 |
| P4 超大文件/日志 | Web Worker 解析、进度、虚拟滚动、日志提取 | 工程化加固 |
| P5 CLI+分享 | CLI（MIT 开源，复用引擎）+ 轻 Node 分享服务（短链/工作区同步/有效期） | 商业化 V1 |
| P6 AI（V2） | AI 三层（L1 本地 / L2 云端配额 / L3 MCP 暴露） | 增值落地（LLM 调用留 stub） |

---

## 9. 已决策汇总（本版新增）

1. **分享服务**：轻 Node 实现，闭源；对外承诺"纯前端，数据不经过你的服务器"。
2. **账号体系**：可选账号——匿名即用（内存/localStorage）；GitHub OAuth 主 + 可选 Google；定位"工作区同步"而非"数据永久存储"。
3. **Graph 布局**：默认力导向 + 树形切换；采样确定性（>500 深度采样前 3 层、>2000 警告建议表格视图），**绝不随机采样**。
4. **定价**：免费(1MB/10链接/7天) / Pro(¥29月·¥199年, 10MB/无限/30天) / Team(¥99月, 50MB/无限/90天+密码)；免费不限额功能只限量限时；>10MB 引导 CLI。
5. **开源**：CLI=MIT（必须开源）、Web 引擎=GPL、分享后端=闭源（开源获客、闭源变现）。

---

## 10. 下一步（进入实现）

P1 启动：搭建 Vite + React 19 + TS 工程骨架；实现多格式解析 → 统一 AST（含源码偏移）→ Web Worker 解析；CLI 与 Web 共用同一解析引擎包（monorepo 或共享 `core` 目录）。

---

## 11. 实施进度（迭代记录）

### P1 引擎 — 已完成 ✅
- monorepo：`packages/core`（引擎，含多格式解析→统一 AST、源码偏移、路径标注、jq 风格查询）、`packages/cli`（commander，query/convert/web，MIT）、`packages/web`（Vite+React19+TS）。
- 自研 JSON 词法分析器（带 `start/end` 源码偏移）、YAML/TOML/CSV 解析、BOM 剥离、格式自动探测。
- Web Worker 异步解析 + 进度回传（`parseWorker.ts` / `useParseWorker.ts`）。
- CLI 实测（含 BOM 文件）、core 单测 13/13、typecheck/build 通过、Chrome MCP 走查树渲染 12 节点 + 隐私文案正确。

### P2 双视图 + 同步 — 已完成 ✅
- **源码视图** `SourceEditor.tsx`：基于 CodeMirror 6（json/yaml 语言高亮、行号、深色主题），支持编辑实时解析、按格式切换语言。
- **树视图** `VirtualTree.tsx`：虚拟化渲染（flatten 可见节点 + 视口裁剪 + overscan），支持折叠/展开、搜索高亮并自动展开命中子树，>2000 节点提示。
- **双视图并排** `App.tsx`：左源码 / 右树；点击树节点 → 选中节点 `start/end` 偏移在源码中用 `cm-node-highlight` 高亮并滚动到可见（JSON 偏移已落地；YAML/TOML/CSV 偏移待补，目前走路径提示）。
- **塑形 / 导出**：格式化、压缩（minify）、排序键（sortKeys 递归、数组保序）；导出 JSON/YAML/TOML(结构回退 JSON)/CSV（toast 反馈、浏览器下载）。
- `core/serialize.ts` 新增 `astToPlain/sortKeys/formatJson/minifyJson/shapeToJson`，core 单测扩至 18/18；web typecheck + build 通过（CodeMirror+yaml+papaparse 已装）。

### 下一步：P3（Graph + Compare + 字段追踪）
- Graph View：基于 react-force-graph 的力导向（默认）+ 树形切换；>500 节点确定性采样前 3 层、>2000 警告建议表格视图（**绝不随机**）。
- Compare 双文档差异；字段追踪：树/搜索定位 → 源码高亮 + 路径揭示。

### P3 Graph + Compare + 字段追踪 — 已完成 ✅
- `core/graph.ts`：`buildGraph`（AST→节点/边，带 depth/label/value/path/offset）、`sampleForGraph`（**确定性深度采样**：>500 节点仅保留前 3 层，绝不随机，结果稳定可复现）、`diffTrees`（按路径结构 diff，added/removed/changed/unchanged，按深度+字典序排序）。
- core 单测扩至 **23/23**（graph：节点/边+深度标签、采样阈值与确定性、diff 四类操作与排序）。
- `web/GraphView.tsx`：**基于 `react-force-graph`（2D canvas + 3D WebGL 运行时切换）** 的高密度图浏览器（2026-08-08 重写为力导向，替代早期自研 SVG 方案）；`d3-force` 在 web worker 中迭代布局，按节点数缩放 charge/link distance；节点按类型着色（object/array 紫蓝、string/number 蓝、boolean 橙、null 灰），hover/重缩放/选中才画标签，折叠对象/数组显示 `+N` 徽标；点击节点 → 经 `findNodeById` 在完整 AST 中定位并高亮源码（含 JSON 偏移）；2D/3D 切换、展开全部/收起全部、下载 PNG。采样/折叠策略仍由 `core/graph.ts` 的 `sampleForGraph` 提供**确定性深度采样**（>500 节点仅保留前 3 层，绝不随机，结果稳定可复现），与布局引擎解耦。
- `web/CompareView.tsx`：A/B 双文本框 + 「比较」按钮（同步解析，错误就近提示），按路径列出 added/removed/changed 并统计数量，changed 显示 `旧→新`。
- `App.tsx`：右侧面板新增「树 / Graph」切换标签；>500 节点时显示黄色采样提示（树/源码仍为完整数据）；新增 `<details>` 折叠的对比视图；导出与塑形保留。
- web typecheck + build 通过（694KB / gzip 222KB，CodeMirror+yaml+papaparse+graph）；dev server 验证 GraphView/CompareView/App 模块均 200 正常 transform。

### P4（待启动）：虚拟滚动强化 + 大文件性能 + 分享/账号闭环
- 树视图已虚拟化（flatten+视口裁剪），P4 重点：>10k 节点交互流畅度、搜索索引、diff 大数据量分页。
- 分享链接服务（轻 Node，闭源）、GitHub OAuth 可选账号（匿名即用内存/localStorage）。
- 路径高亮动画、键盘导航、列视图（大数组）。

### P4（本轮交付子集）— 已完成 ✅
围绕「纯前端、数据不经服务器」承诺，优先落地了**本地分享链接**与**真实解析进度**：
- `web/share.ts`：基于浏览器内置 `CompressionStream('gzip')` + base64url，将文档压缩后编码进 URL 片段（`#doc=…`）。`buildShareUrl` / `readShareFromUrl` / `shareUrlLength`。**无服务器、数据仅存在于链接本身**。实测 5KB 文档压缩后 URL 片段仅 179 字符（约 4% 体积）。
- `web/ShareView.tsx` + App「分享」标签：生成链接、一键复制、链接长度提示；超 60k 字符给出「改用 CLI/本地保存」降级建议。
- App 挂载时 `readShareFromUrl()` 自动解码 `#doc=` 并还原源码+格式（收件人打开即本地还原）。
- `parseWorker.ts`：解析进度改为**真实可测的首遍**——JSON 走 `tokenize` 按 token 分桶上报（O(n) 真实工作量），非 JSON 给定轻量进度；解析在 Worker 内异步 `await tick()` 让进度消息刷新，不再用假循环。
- 大文件性能：树视图已在 P2 虚拟化；本轮 P4 主体（>10k 节点交互、diff 分页、OAuth 账号后端、分享服务端）体量较大，**未在本轮全部完成**，计划另开迭代。

### 代码分割（bundle 优化）— 已完成 ✅
- `App.tsx` 将 `GraphView` / `CompareView` / `ShareView` 改为 `React.lazy` 动态导入（`Suspense` 包裹），真正按需加载。
- `vite.config.ts` 增加 `manualChunks`：CodeMirror（含 @lezer）、react、yaml/toml/papaparse 各自独立 vendor chunk；`chunkSizeWarningLimit` 提到 800。
- 效果（vite build 实测）：主 `index` chunk 由 **693 KB（gzip 222 KB）→ 23 KB（gzip 8.89 KB）**；CodeMirror 320 KB / react 194 KB / formats 154 KB / parseWorker 153 KB 均为独立可缓存块；懒视图 Graph 3.4 KB、Share 3.2 KB、Compare 2.5 KB。首屏关键路径体积下降约 25 倍，警告消除。

### P5 分享服务端 + 可选 GitHub 账号（工作区同步）— 已完成 ✅
- **新增 `server/`（闭源，UNLICENSED）**：轻 Node + Express 服务，纯 JS JSON 文件存储（无原生依赖，零编译门槛），短链 TTL（默认 7 天）+ 工作区 TTL（默认 30 天），定时 purge。
  - `src/db.ts`：轻量 JSON 存储（shares / workspaces / accounts）。
  - `src/server.ts`：`POST /api/share`（限 4MB，返回 `/s/:id`）、`GET /api/share/:id`、`GET /s/:id`（小段 redirect 页面）、`GET|PUT|DELETE /api/workspace/:id`、`GET /api/workspace`（列表）、`GET /api/health`。
  - `src/oauth.ts`：`/api/oauth/login|callback|me|logout`，可选 GitHub OAuth（仅读用户名+头像，HMAC 签名会话 cookie，支持 `GITHUB_ALLOWED_USERS` 白名单）；未配置密钥时自动关闭、web 仍纯本地可用。
  - 配置见 `server/.env.example`；`README.md` 说明闭源协议与运行方式。
- **前端接入（`packages/web`）**：
  - `share.ts`：`buildShareUrl`（本地 `#doc=` gzip）+ `buildServerShareUrl`（POST 服务端）+ `readShareFromUrl` 同时识别 `#doc=` 与 `/s/:id`(或 `#/s/:id`) 短链；`setApiBase` 供 workspace hook 设置服务基址。
  - `useWorkspace.ts`：可选账号 hook（anonymous 默认；配置 `VITE_API_BASE` 后启用 GitHub 登录、工作区列表/保存/读取/删除）。
  - `ShareView.tsx`：三部分 UI——① 纯前端分享链接（推荐，默认）、② 服务端短链（需 `VITE_API_BASE`）、③ 工作区同步（GitHub 登录态下保存/读取/删除文档）。
  - `App.tsx`：ShareView 增加 `onDocument` 回调，工作区读取可回填编辑器并重新解析；挂载时 `readShareFromUrl` 已兼容服务端短链。
- **验证**：server `tsc` 通过；启动后 `POST /api/share`→`GET /api/share/:id` 往返正确，`/api/health` 正常；web `tsc --noEmit` 通过、`vite build` 通过且分包正确。OAuth 因未配密钥在本环境 `oauth=false`（预期）。
- **已知取舍**：服务端短链读取依赖 `VITE_API_BASE` 指向分享服务（跨域需服务端 CORS，当前同源部署或网关代理更佳）。

### 工作区冲突合并（多设备乐观并发）— 已完成 ✅（2026-08-13）
在 P5 工作区雏形之上补齐**多设备冲突检测与合并**，消除"静默覆盖"风险：
- **服务端版本号（`server/src/db.ts` + `server/src/server.ts`）**：`workspaces` 表新增 `version INTEGER NOT NULL DEFAULT 1`（建表后 `ALTER TABLE ... ADD COLUMN` 幂等兼容旧库）。`upsert` 写库时 `version = (existing?.version ?? 0) + 1`；`GET /api/workspace/:id` 返回 `version`。
- **乐观并发冲突（`PUT /api/workspace/:id`）**：请求体可选 `baseVersion`；若服务端当前 `version !== baseVersion` → 返回 `409 conflict` + `conflict:{version,name,source,format}`（服务端最新内容），客户端据此裁决而非盲目覆盖。`force` 覆盖时不带 `baseVersion` 跳过校验。
- **前端 hook（`packages/web/src/useWorkspace.ts`）**：`save()` 现返回 `SaveResult`（`{ok,version}` 或 `{ok:false,conflict}`），接受 `baseVersion`/`force`；`load()` 返回带 `version`/`source`；`refreshList` 映射 `version` 并补 `credentials:'include'`。
- **冲突 UI（`packages/web/src/WorkspaceSync.tsx`，新组件）**：接进 `ShareView` 第④部分「工作区同步」。提供 ID/名称输入、保存到工作区、从工作区加载、我的工作区列表（显示版本与更新时间、可删除）；`save` 返回 `conflict` 时弹**冲突裁决弹窗**：左「服务器版本」vs 右「我的版本」并排 `pre` 展示，三按钮「用服务器版本 / 用我的版本（覆盖 force）/ 取消」。组件内 `versionsRef` 记每个 id 已保存版本，作为下次 save 的 `baseVersion`。
- **i18n / 样式**：`i18n.tsx` 新增 `share.workspace` + 全部 `ws.*`（中/英）；`styles.css` 补 `.ws-sync`/`.ws-modal`/`.ws-diff` 等冲突面板样式。
- **验证**：`packages/web` `tsc --noEmit` ✅ + `vite build` ✅（ShareView chunk 含 WorkspaceSync）；`server` `tsc` ✅（顺手修复：装齐 `@types/node` 后 db/server 通过）。
- **设计取舍**：冲突**不自动深合并**（JSON 语义合并风险高、易丢数据），改为「并排展示 + 用户裁决」——符合计划「冲突合并」让用户在冲突时做决策的目标；需要行级 diff 辅助时由用户复制两段到编辑器手动合并。OAuth 真实公网走查仍依赖真实凭据（见下表 🟡）。

### 分享付费档（配额与鉴权）— 已完成 ✅（2026-08-13）
把计划 §7 的「免费/Pro/Team」定价落地为**服务端配额引擎 + 分享创建鉴权**（真实支付网关未接，档位由环境变量 allow-list 模拟，留作后续接计费）：
- **档位模型（`server/src/plans.ts`，新）**：`PLANS` 定义三档配额——`free`(1MB/10链接/7天/无密码)、`pro`(10MB/无限/30天)、`team`(50MB/无限/90天/支持密码)；`ANON_QUOTA=free`、`ABSOLUTE_MAX_BYTES=50MB`（滥用硬上限）；`tierFromAllowList(login)` 读 `PRO_LOGINS`/`TEAM_LOGINS`（逗号分隔 GitHub 登录，小写）覆盖默免费档；`formatBytes` 辅助。
- **账号档位（`server/src/db.ts` + `oauth.ts`）**：`accounts` 表加 `tier TEXT NOT NULL DEFAULT 'free'` + `plan_expires_at`（建表后 `ALTER TABLE` 幂等）；`accounts.upsert` 带 tier、`get` 返回 tier、`setTier` 供后续计费写回；OAuth callback 据 allow-list 落 tier；`/api/oauth/me` 返回 `tier`。
- **分享鉴权（`server/src/server.ts`）**：`shares` 表加 `owner TEXT` + `password TEXT` 并加索引/计数 `countFor(owner)`；`POST /api/share`：未登录走 `ANON_QUOTA`，登录走其档位配额——超 `maxBytes` 返回 `413 {code:'quota_bytes',tier,maxBytes}`、超 `maxShares` 返回 `409 {code:'quota_shares'}`、密码保护仅 `team` 允许否则 `402 {code:'quota_password'}`；TTL 按档位 `ttlDays` 覆盖全局 `SHARE_TTL`；密码用 `sha256('jsona:'+pw)` 哈希存储。`GET /api/share/:id` 支持 `?pw=`/`x-share-password` 校验后返回源。`GET /api/share/quota`（登录）返回当前档位用量。`/api/health` 返回 `plans` 三档说明。
- **前端（`packages/web`）**：`share.ts` 的 `buildServerShareUrl` 重写——支持 `ServerShareOpts{alertQuotaUp,apiBase,password}`，配额拒绝(413/409/402)时抛带「升级 /pricing」提示的 Error；`ShareView` 第②部分新增「生成服务端短链」按钮 + 配额错误提示条（`.share-quota-err`）；`useWorkspace` 暴露 `tier`，`WorkspaceSync` 列表头显示「login · TIER」；`i18n.tsx` 加 `share.serverCreate`/`share.serverFail`/`ws.tier`（中/英）；`styles.css` 补 `.server-share`/`.share-quota-err`。
- **验证**：`packages/web` `tsc` ✅ + `vite build` ✅；`server` `tsc` ✅。
- **已知取舍（未做）**：① 真实支付/订阅计费未接（档位靠 `PRO_LOGINS`/`TEAM_LOGINS` 环境变量模拟，方便自托管运营；接 Stripe 后只需改 `tierFromAllowList`）；② 密码保护的分享**前端读取交互**仅后端 `/api/share/:id` 校验可用，但 `ShareView` 未加「输入密码查看」表单（短链页 `/s/:id` 读源时未传 `?pw=`），密码创建可用、消费端 UI 留待补；③ 档位变更（升级/降级）无后台页面，仅环境变量 + 预留 `setTier`。

### AI 三层架构（L1 本地 / L2 云端 LLM / L3 MCP）— 已完成 ✅（2026-08-13）
把计划 §5.3 的「AI 处理」从占位按钮落地为**三层架构**，按用户选择一次性完成本地能力 + 云端端点骨架（LLM 调用留 stub）+ MCP 暴露。隐私默认仅发结构/片段且每次明示同意，契合「数据纯本地」卖点。

- **L1 本地 AI（纯前端、零后端、免费）**：
  - `packages/web/src/ai/inspect.ts`（新，纯函数，基于 `@jsona/core` 的 `JsonNode`）：`inferSchema`（schema 推断 + 类型分布 + 空值/可空字段路径 + 字段清单）、`structuralSummary`（结构摘要，作为发往云端的 payload）、`explainNode`（一句话结构说明）、`findPathsLike`（按关键字找路径）。完全离线、无任何 network 调用。
  - `packages/web/src/ai/AiPanel.tsx`（新）：点 `topbar.ai` 按钮弹出的面板，三标签——「本地分析」(根类型/节点数/深度/空值/类型分布/字段清单)、「问 AI」(L2 输入框)、「解释」(选中节点说明 / 路径查找)。`App.tsx` 死按钮改为打开 `ai-overlay` 面板，挂载 `selected` / `result` / `tier` / `onApplyToDocument`(→ `runParse`)。
  - `i18n.tsx` 补 `ai.*`（中/英，含 consent 文案、localNote、cloudNote）；`styles.css` 补 `.ai-*` 全套暗色适配样式。

- **L2 云端 LLM（后端端点 + 配额，BYOK 真实转发）**：
  - `server/src/ai.ts`（新）：`AI_PLANS`（free=0 即 AI 为付费增值、pro=200/月、team=无限）、`aiQuotaFor`/`getAiUsage`/`checkAiQuota`/`recordAiUsage`（按月 `ai_usage` 表，建表幂等）、`callLlm(req, owner)`（**BYOK 转发**：经 `getByokForOwner(owner)` 读该账号自配密钥，命中则 `byok.callProvider` 真实调用；未配置则返回引导文案提示去「AI 设置」填 key；**绝不接收原文**——隐私边界由前端保证、后端契约也只声明 summary）。
  - `server/src/byok.ts`（新）：AES-256-GCM 加解密（`encryptKey`/`decryptKey`，主密钥 `BYOK_MASTER_KEY`，缺失时退化为进程内随机密钥并告警）、`maskKey`、`callProvider(settings, prompt)`（支持 `openai` / `anthropic` / `openai-compatible` 自定义 baseURL，解析 chat/completions 与 messages 两类响应，抽取第一个 ``` 代码块作 `generatedSource`）、`SUPPORTED_PROVIDERS`。
  - `server/src/server.ts`：`POST /api/ai`（鉴权 → 有 BYOK 则**跳过档位配额**、否则 `checkAiQuota` 超 402 `{code:'quota_ai'}` → `callLlm(...,login)` → `recordAiUsage` → 返回 `{answer,generatedSource?}`）、`GET /api/ai/quota`（含 `byok:boolean`）、`GET /api/ai/settings`（返回 provider + 掩码 key，**绝不回传明文**）、`PUT /api/ai/settings`（校验 provider、加密储 key；空 apiKey 保留原 key、空 provider 清除全部）。`accounts` 表新增 `ai_provider`/`ai_api_key`(密)/`ai_model` 三列（`db.ts` 幂等迁移 + `setAiSettings`）。
  - 前端 `packages/web/src/ai/api.ts`：`askCloudAi`（`credentials:'include'`，仅发 `summary`+`selectedPath`，抛 `AiQuotaError(402)`）、`fetchAiQuota`、`getAiSettings`/`putAiSettings`；`AiPanel.tsx`「问 AI」标签加「⚙ AI 设置」按钮（仅登录可见）→ 弹窗选 provider / 填 apiKey / 模型 / openai-compatible 的 baseURL，保存走 `PUT /api/ai/settings`。`styles.css` 补 `.ai-settings-btn`/`.ai-modal-*` 暗色弹窗样式；`App.tsx` 传 `login={workspace.login}`。

- **L3 MCP（jsona 作为 MCP Server，开源资产，免费）**：
  - **初版（2026-08-13，已废弃）**：`server/src/mcp.ts` 手写 stdio JSON-RPC（`initialize`/`tools/list`/`tools/call`），三个工具——`jsona_query`(按 JSON-path 查)、`jsona_schema`(结构摘要)、`jsona_convert`(格式互转)。直接复用 `@jsona/core` 的 `parse/queryPath/astToFormat`，**无 HTTP、无 DB、无出网**，文档不出本机，契合「数据零上传」。
  - **SDK 标准化 + CLI 联动（2026-08-13，当前版）**：MCP server **从闭源 server 迁移到开源 CLI 包**（`jsona-view`），用**官方 `@modelcontextprotocol/sdk`**（v1.30.0，peer `zod` v4）重写：
    - `packages/cli/src/mcp.ts`（新）：`createMcpServer()` 用 `McpServer` + `registerTool`（`inputSchema` 用 zod，含 title/description）注册 `jsona_query` / `jsona_schema` / `jsona_convert`；`runMcpServer()` 用 `StdioServerTransport` 连接；`printManifest()` 输出工具目录 JSON。工具 handler 返回 MCP 标准 `{content:[{type:'text',text}]}` 结构。
    - `packages/cli/src/index.ts`：注册 **`jsona mcp`** 子命令（`--manifest` 打印目录后退出；`--http` 走 HTTP transport；否则 stdio server 长驻）。`SUBCOMMANDS` 集合加入 `mcp`。
    - CLI 安装依赖：`@modelcontextprotocol/sdk` + `zod`（4.4.3）。
    - `server/src/mcp.ts` **已删除**；`server/package.json` 移除 `@jsona/core` 依赖（server 不再需要 core）；`server/src/server.ts` 的 `GET /api/mcp/manifest` 改为指向 `jsona mcp` + `npm i -g jsona-view`（仅作发现入口，真实服务跑在 CLI）。
  - **HTTP/SSE transport（2026-08-13 追加，远程 Agent 用）**：`jsona mcp --http [--port N] [--host H]` 启动 **MCP Streamable HTTP** server（默认 `127.0.0.1:3939/mcp`，CORS 全开，`GET`=SSE 流 / `POST`=JSON-RPC / `DELETE`=会话终止）：
    - 用官方 `StreamableHTTPServerTransport`（stateful per-session），node:http 手写路由，不引入 express 顶层依赖（SDK 内部自带 `@hono/node-server` 转换）。
    - **关键坑**：SDK 的 `Protocol`（`McpServer`）**一个实例只能连接一个 transport**，多 session 复用同一 `McpServer` 会抛 `Already connected to a transport`——因此**每个 session 独立创建 `createMcpServer()` + transport**（工具是无状态纯函数，按 session 建实例安全）；且生成的 `sessionId` 需在 `handleRequest` **之后**才可用，注册 Map 必须放在 handle 完成后，否则第二个请求（notifications）找不到 session → `Server not initialized`。
    - `--host 0.0.0.0` 可暴露到局域网/公网（建议放反向代理 + 鉴权）。
  - **OAuth 授权流（2026-08-13 追加，远程 Agent 鉴权）**：`jsona mcp --http --auth` 为 HTTP 端点加 **OAuth 2.1 授权码 + PKCE** 保护：
    - 新增 `packages/cli/src/mcp-oauth.ts`：实现 SDK 的 `OAuthServerProvider` + `OAuthRegisteredClientsStore` 契约（动态客户端注册、授权码 PKCE 校验、token/refresh 签发）。token 用 `jose` 签 **HS256 JWT**（含 `client_id`/`scope`/`exp`/`jti`），refresh token 双签发并记录 jti 撤销。状态持久化到 `~/.jsona/oauth.json`（signing key + clients + authCodes + refreshTokens），重启不丢。
    - `mcp.ts` 的 `runAuthHttpMcpServer`：用 `mcpAuthRouter` 挂标准端点（`/.well-known/oauth-authorization-server`、`/register`、`/authorize`、`/token`、`/revoke`），`/mcp` 前挂 `requireBearerAuth`。
    - **关键坑**：① SDK 的 `createMcpExpressApp` 会**全局挂 `express.json()`**，消费 `/mcp` 的 raw body，导致 `StreamableHTTPServerTransport.handleRequest` 报 `Parse error: Invalid JSON`——必须**手动 `express()`** 且 `/mcp` 注册在 `express.json()` **之前**，JSON parser 只留给 OAuth 端点；② token 端点默认要求 `client_secret_post`，token 交换必须带 `client_secret`（SDK 元数据里 `token_endpoint_auth_methods_supported: ["client_secret_post","none"]`）；③ Streamable HTTP 的 POST 响应可能是 **SSE**（`event: message\ndata: {...}`），客户端解析要兼容 JSON 与 SSE 两种格式。
    - 依赖：`express` + `ws`（CLI 顶层）+ `jose`（签 JWT）+ `@types/express`/`@types/ws`。
  - **WebSocket transport（2026-08-13 追加）**：`jsona mcp --ws` 启动 **WS server**（`ws://<host>:<port>/`，subprotocol `mcp`）：
    - SDK 只提供**客户端** WS transport（`WebSocketClientTransport`），**无 server 端**——自实现 `WsTransport` 适配 `Transport` 接口（`start/send/close` + `onmessage/onclose/onerror`），每连接一个 `McpServer`。`ws` 包处理 upgrade，纯 WS 无 REST 面。
  - **Client 能力：sampling / roots（2026-08-13 追加）**：MCP 的 **sampling**（server→client LLM）与 **roots**（server 读 client 暴露的文件根）是 **client 能力**，server 通过底层 `server.server`（SDK `Server`）主动发起请求。`McpServer` 高层未封装这两方法，故工具回调内直接调 `server.server.createMessage()` / `server.server.listRoots()`：
    - 新增工具 **`jsona_ask`**（sampling）：把文档原文 + 问题发给 client LLM（`sampling/createMessage`），让 client 侧的模型对 JSON/YAML/TOML/CSV 做解释/摘要。调用前用 `server.server.getClientCapabilities()` 检查 `sampling`，缺失则返回 `isError` 友好提示（"此 client 未声明 sampling 能力"）。支持 `systemPrompt` / `maxTokens`（1–4096，默认 512）/ `format` 探测。
    - 新增工具 **`jsona_roots`**（roots）：调用 `server.server.listRoots()` 列出 client 暴露的 `file://`/URI 根（client 允许 server 读取的资源集合）。同样先做 `roots` 能力检查，缺失则友好降级。
    - 两个工具在**任意传输**（stdio/http/auth/ws）下都可用——它们是 client 能力，与 server 传输无关。stdio/WS 默认 client 若不支持即返回降级提示，HTTP/OAuth 客户端（远程 Agent）只要声明能力即可用。
  - **MCP Resources / Prompts（2026-08-13 追加）**：补齐"工具 + 资源 + 提示"三大件，使 jsona 成为规范完整的 MCP server。
    - **Resources**：`jsona://help` 静态资源（能力说明 Markdown，含 Tools/Resources/Prompts 清单 + 隐私说明）；`jsona://samples/{format}` 资源模板（`format` ∈ json/yaml/toml/csv，read 返回对应格式样例文档，用于让 Agent 先 `read` 再 `jsona_convert`/`jsona_query`；非法 format 抛错）。通过 `McpServer.registerResource` 注册，模板用 `ResourceTemplate`（`complete.format` 提供格式自动补全）。
    - **Prompts**：`jsona_explain`（参数 `format`/`question`，引导 LLM 用 `jsona_schema`+`jsona_query` 解释文档）、`jsona_validate`（参数 `source`/`format`，引导 LLM 校验文档）。通过 `registerPrompt` + `argsSchema`（zod）注册，返回标准 `messages[]`。
    - **roots/list_changed 缓存（项 3）**：SDK 1.30 **未暴露** server 端 `notifications/roots/list_changed` 订阅钩子（`setNotificationHandler` 不存在，`Protocol` 非 EventEmitter），故无法实现规范的"client 推送变更→server 缓存刷新"。退而实现**进程内 TTL 缓存（30s）**：`jsona_roots` 增加 `refresh` 参数，TTL 内复用缓存、超时才重新 `roots/list`；`refresh:true` 强制刷新。文档中已标注该 SDK 缺口。
  - **使用方式**：
    - **本地**（Claude Desktop / Cursor / VS Code Copilot / 自建 client）：注册 stdio `{command:'jsona', args:['mcp']}`。
    - **远程 Agent**：`jsona mcp --http --port 3939 --host 0.0.0.0`，客户端以 `http://<host>:3939/mcp` 作为 Streamable HTTP 端点连接；浏览器端 Agent UI 可跨域直接 POST（CORS 已放行）。
    - **远程受保护**：`jsona mcp --http --auth --port 3939 --host 0.0.0.0 [--oauth-state-file ~/.jsona/oauth.json] [--issuer-url https://host]`，客户端走标准 OAuth 授权码流程（动态注册 → 授权 → PKCE → token → Bearer 调 /mcp）。
    - **WS**：`jsona mcp --ws --port 3939`，客户端以 `ws://<host>:3939` 连接。

- **验证**：`packages/web` `tsc --noEmit` ✅ + `vite build` ✅（仅 monaco 体积警告，与本次无关）；`server` `tsc` ✅（含删 mcp.ts 后重新编译，`dist/mcp.js` 已清理）；**官方 SDK Client 端到端实测**——
  - **stdio**：`Client` + `StdioClientTransport` 连接 `jsona mcp`，握手成功（`serverInfo {name:'jsona', version:'0.2.0'}`）、`listTools` 三工具、`jsona_schema`（`{"a":1,"b":null,"users":[{"email":"x@y.z"}]}` → rootKind=object/nodeCount=6/nullCount=1）、`jsona_query`（`users.0.email` → `x@y.z`）、`jsona_convert`（JSON→YAML）全部正确；`jsona mcp --manifest` 正常；server `/api/health` + `/api/mcp/manifest` 200。
  - **http**：`Client` + `StreamableHTTPClientTransport` 连接 `jsona mcp --http --port 3939`，握手、listTools、三工具调用（schema/query/convert）全部正确，session 复用正常（CONNECTED → 多工具顺序调用）。
  - **ws**：`Client` + `WebSocketClientTransport` 连接 `jsona mcp --ws`，握手（serverInfo jsona v0.2.0）、listTools 三工具、schema/query 调用全部正确。
  - **oauth**：两轮验证——(a) 手写完整流程：动态注册 → authorize 302（PKCE S256 + state 透传）→ token 200（access+refresh）→ 无 token 调 /mcp 401 → Bearer initialize 200（serverInfo + session）→ tools/list 200；(b) **官方 SDK Client**（`StreamableHTTPClientTransport` + `authProvider` 供 token）：握手、listTools、query 调用全部正确。
  - **sampling / roots**：官方 SDK `Client` 连接 `jsona mcp`（stdio）——(a) **无能力 client**（不声明 sampling/roots）：`jsona_ask` / `jsona_roots` 均返回 `isError:true` + 友好提示（能力检查生效）；(b) **有能力 client**（注册 `setRequestHandler(CreateMessageRequestSchema)` mock 返回 LLM 文本、`setRequestHandler(ListRootsRequestSchema)` 返回 2 个 `file://` 根）：`jsona_ask` 走通 `sampling/createMessage` 返回 LLM 文本、`jsona_roots` 走通 `roots/list` 返回 2 个 root。两种场景均验证 `listTools` 含 5 工具（query/schema/convert/ask/roots）。
  - **resources / prompts / roots-cache**：官方 SDK `Client`（`listResources`/`listResourceTemplates`/`readResource`/`listPrompts`/`getPrompt` + mock sampling/roots）——`jsona://help` 静态资源可读（含 Tools 段）、`jsona://samples/{format}` 模板被识别、read `jsona://samples/json` 返回合法 JSON、非法 format 被拒；`jsona_explain`/`jsona_validate` 已注册且 `getPrompt` 返回正确引导消息；`jsona_roots` 首次 `cached:false`、二次 `cached:true`（TTL 命中）、`refresh:true` 后 `cached:false`（强制刷新生效）。

- **注意（踩坑记录）**：① 强杀 server 进程会在 `server/data/` 残留 `share.db.lock/` 目录，导致下次启动 `database is locked`——删除该锁目录即可（DB 无损）；② 本机 curl 默认走 IPv4 `127.0.0.1`，而 server `app.listen` 监听 `::`，需用 `http://[::1]:8787` 测试。
- **已知取舍（未做）**：① 真实**自有** LLM 未接——jsona 不托管模型、不对 token 计费，改为 **BYOK**（用户填自己的 key，后端零成本转发，2026-08-13 已落地）；接 jsona 自有 SaaS key 需另做密钥管理与计费，暂不做；② 「整文档发送」开关未做，目前仅发结构摘要（符合默认隐私边界）；③ `generatedSource` 由 `callProvider` 抽取首个 ``` 代码块产出，但前端「应用到文档」按钮当前仍未接（仅 local 解释链路消费 runParse）；④ MCP 初版手写 stdio JSON-RPC 已**废弃替换为官方 `@modelcontextprotocol/sdk` 标准化实现**（2026-08-13，见 L3 小节），支持 **stdio + Streamable HTTP（SSE）+ OAuth 2.1 授权流 + WebSocket 四传输**；OAuth 的 `authorize` 为**自动批准**（headless，无人工 consent 页，生产部署建议改成手动确认）；WS 传输无 OAuth 保护（与 stdio/WS 一致性：WS 是纯通道，鉴权留待 HTTP 同构扩展或部署层）；MCP 未做 SSE（旧版）。sampling/roots/resources/prompts 已实现（2026-08-13，见 L3 小节）。**已知 SDK 缺口**：SDK 1.30 未暴露 server 端 `notifications/roots/list_changed` 订阅钩子，故 roots 仅实现进程内 TTL 缓存（30s，可 `refresh` 强制刷新），真正的"client 推送变更→server 自动刷新"需升级 SDK 后才可补全。**BYOK 安全取舍**：密钥以 AES-256-GCM 加密落库，`BYOK_MASTER_KEY` 未设时退化为进程内随机密钥（重启后旧密文不可解密，仅 obscuration）；GET `/api/ai/settings` 只回掩码，明文 key 仅在该次 PUT 请求内存中短暂存在、绝不回传。

- **附带修复（`server/src/db.ts` 幂等迁移）**：本轮运行时冒烟发现既有 bug——`ALTER TABLE shares ADD COLUMN owner` 在表已含 `owner` 列时重复执行抛 `duplicate column name`，导致 server 启动即崩（分享付费档引入该列后，服务端从未以新 schema 成功启动过）。修复为 `hasColumn(table,col)` + `addColumnIfMissing` 检查后再 ALTER，`CREATE TABLE IF NOT EXISTS` 建表与列迁移彻底分离，重启任意次数安全。验证：clean DB 与含旧表 DB 均正常启动。


- **树视图键盘导航**：`VirtualTree` 容器可聚焦，支持 ↑/↓(j/k) 移动选中、→/l 展开并进入、←/h 折叠或回到父级、Enter/空格 选中或切换展开；被选中的行带 `focused` 轮廓并自动滚动入视。
- **搜索索引**：查询匹配时强制展开命中节点（即使父级被折叠），命中行 `match` 高亮；虚拟化保持 O(n) 扁平化。
- **大数组/树性能**：行 `contain`/固定高度虚拟化（ROWS 常驻 DOM 极少）；>10k 节点滚动流畅。
- **路径高亮动画**：选中树节点时源码高亮以蓝色脉冲（hlFlash 0.6s）过渡到稳态琥珀色；`SourceEditor` 增加 `highlightVersion` 在每次新选中重放动画。
- **diff 大数据量分页**：`CompareView` 增加过滤器（全部/新增/删除/修改 + 计数 chip）与每页 200 条分页器，避免万级 diff 一次性渲染卡顿。
- **列视图（JSON Hero 风格）**：新增 `ColumnView.tsx`（懒加载块 1.94KB），三栏钻取浏览大数组/深层对象，选中与树视图/`selectedId` 双向联动；App 增加「列」标签。

### 分享服务端部署与 CORS（补充）— 已完成 ✅
- `server/src/server.ts` 增加 CORS 中间件，按 `PUBLIC_ORIGINS`（逗号分隔，或 `*`）放行跨域，支持凭据与预检 OPTIONS。
- 新增 `server/Dockerfile`（node:20-alpine，纯 JS 构建，挂载 `/app/data` 卷）。
- `server/README.md` 扩充：构建/运行、Docker 部署、web 端 `VITE_API_BASE` 对接、GitHub OAuth 注册与回调配置、安全注意事项。
- **验证**：server `tsc` 通过；CORS 冒烟测试确认带 Origin 请求返回正确 `Access-Control-Allow-Origin` 头；web `tsc --noEmit` + `vite build` 通过，列视图独立分包。

### P7 列视图内联编辑 / 树面包屑 / 源码↔树反向定位 / 服务端 SQLite + 限流 — 已完成 ✅
- **列视图内联编辑与复制**：新增 `packages/web/src/edit.ts`，基于节点 `start/end` 源码偏移对源文本做外科式拼接，保留注释/空白/键顺序；`ColumnView` 双击或点击编辑图标进入输入框，Enter 提交、Esc 取消，字符串/数字/布尔/null 自动识别字面量格式。
  - 检查器支持「复制路径」「复制值」「复制原文」，带 toast 反馈；`copyText` 兼容 `navigator.clipboard` 与隐藏 textarea 降级。
- **树视图面包屑**：新增 `packages/web/src/Breadcrumb.tsx`，显示从根到当前节点的路径链，深路径自动折叠中间层级，每个祖先可点击跳转，并支持复制当前路径。
- **源码↔树双向定位**：
  - 新增 `packages/web/src/locate.ts`：`nodeAtOffset` 从源码偏移定位最深包含节点，`ancestorIds` 收集祖先用于自动展开。
  - `SourceEditor.tsx` 新增 `onCursor` 回调，在光标移动时报告偏移；App 通过 `nodeAtOffset` 反向选中树节点，并避免自身滚动造成的事件循环。
  - `VirtualTree` 外部选中时自动展开祖先链并滚动入视。
- **服务端 SQLite 与限流**：
  - `server/src/db.ts` 改为 SQLite（`node-sqlite3-wasm`，纯 WASM 无需原生编译），迁移旧 JSON 文件，支持 WAL + TTL purge + 持久化重启。
  - `server/src/ratelimit.ts`：按 IP 的写/读/OAuth 独立限流桶，计数器存 SQLite 防重启重置；超过限制返回 429 + `Retry-After` + `X-RateLimit-*` 头。
  - `server/src/server.ts`：增加最小 cookie 解析（修复 `req.cookies` 未定义导致 OAuth 失败）、`MAX_SHARE_BYTES` 超大文档拒绝、每账号 `MAX_WORKSPACES` 上限、短链 id 转义防 XSS、全局错误处理；更新 `.env.example` 与 README。
- **端到端浏览器走查（Chrome CDP）**：用本机 Chrome headless + Chrome DevTools Protocol 驱动 16 项断言，全部通过：应用加载、源码/树/列视图渲染、面包屑、树→源码高亮、源码→树反向定位、列视图钻取、检查器复制按钮、列视图内联编辑并写回源码、未编辑内容保持、键盘导航、无运行时异常。
- **验证**：`packages/web` `tsc --noEmit` + `vite build` 通过；`server` `tsc` 通过；SQLite 持久化与限流冒烟测试 10/10 通过；E2E 走查 16/16 通过。

### P8 搜索联动 / 键盘导航 / 非 JSON 偏移补全 / ReactFlow 图谱 — 已完成 ✅
- **搜索结果分页联动**：新增 `packages/web/src/SearchResults.tsx`，树搜索框（`treeQuery`）非空时在左栏渲染「搜索结果 N 条」分页列表（默认每页 50），每行显示 键 / 路径 / 值，点击即 `onSelect`（联动选中树节点 + 源码高亮）。App 左栏由原先始终渲染 `VirtualTree` 改为：有查询词时显示 SearchResults，否则显示 VirtualTree；点击结果自动切回树视图并选中。
- **CompareView diff 行联动**：`CompareView` 新增 `onSelect` 属性，diff 行（新增）变为可点击，按 `.path` 在对应 A/B 原文中 `queryPath` 定位节点并回传主视图做树+源码高亮（diff ↔ 源码双向联动）；行样式 `.diff-row.clickable`。
- **ColumnView 键盘导航**：`ColumnView` 现为 `tabIndex=0` 可聚焦树，`onKeyDown` 支持 ↑/↓ 列内移动、← 上钻/回根、→ 下钻容器或进入下一列、Enter 选中、Home/End 跳首尾、Esc 回上一列、字母键按首字符跳转；聚焦行 `.cv-row.focused` 带描边并 `scrollIntoView`，点击同步更新键盘焦点。
- **树→源码高亮补全（YAML/TOML/CSV）**：`packages/core/src/parsers/offsets.ts` 新增 `backfillOffsets(root, src)`，对经 `valueToAstRoot` 得到的非 JSON AST（YAML/TOML/CSV）做源码扫描回填 `start/end`（对象 `{`/`}`、数组 `[`/`]`、标量字面量、对象 key 文本）；`core/src/index.ts` 的 `parse()` 在 `format!=='json'` 时调用，使 tree→source 高亮在四种格式下均可用（JSON 仍用 jsonc-parser 的精确偏移）。核心冒烟：yaml 9 节点/6 带偏移、toml 5/3、csv 9/6、json 7/7。
- **ReactFlow 可视化（图谱 Tab）**：新增 `packages/web/src/FlowView.tsx`（依赖 `reactflow`），将解析 AST 转为有向树图（按深度分列、DFS 排序、超 600 节点裁剪），节点按类型着色、含父→子边与 MiniMap/Controls/Background；支持点击节点 `onSelect` 联动树+源码、外部 `selectedId` 高亮回显。`App` 新增「图谱」Tab（懒加载，独立分包 ~1.9kB，reactflow 归入 react chunk），`RightView` 增加 `'flow'`。
- **验证**：`packages/core` `tsc` 通过；`packages/web` `tsc --noEmit` + `vite build` 通过（FlowView 正确分包）；CDP 端到端走查 5/5 通过（应用挂载、文件注入解析、图谱 Tab 渲染 ReactFlow 节点、搜索结果分页面板命中、无运行时异常）。

### 待启动（后续迭代）
- OAuth 真机走查（GitHub App 回调需公网可达，本环境未实测）。
- 大文件性能（虚拟滚动强化）、CLI 同引擎、导出格式扩充。
- ColumnView 键盘导航的跨列多选 / 多选删除可后续增强。

---

## 10. 参考竞品要点（已调研）

- **Treease**：图形视图 + 对比 + 编辑器预览 + CLI + 多格式；免费档有限额（图形≤5/月、大文件≤3/月、AI≤3/月），Pro 档放宽。
- **JSON Hero**（jsonhero.io）：三维浏览（列视图/树/预览卡片），免费。
- **JSON Editor Online**：双栏树+文本，格式化/导出，免费+订阅。
- **jq / fx / gron**：CLI 优先，无 GUI。

**我们的差异化机会**：Treease 图形+双向同步体验好但偏订阅且核心能力限额；jsona 做**开源/免费、本地优先、核心能力全免费**，仅云端增值收费，并以"数据纯本地、零上传"建立隐私信任。


## 项目状态总览

根据 `开发计划.md` 与代码实际核对，已完成 **P1–P8 全部里程碑**：多格式解析引擎、双视图+同步、Graph/Flow/Compare/Share/Column 视图、虚拟滚动、搜索、键盘导航、双向定位、分享服务端（含 OAuth/限流/SQLite）、CLI 完善（含 `serve`）、i18n 框架。核心能力基本健全。

**本轮（2026-08-13）已追加完成的 5 项**（此前会话遗留的体验细节）：
1. **列视图横向滚动自动跟随**：下钻/回退/外部选中展开路径时，`.cv-scroller` 平滑滚动到最右（最新）列。
2. **下载按格式真实序列化**（修复一直下 `.txt` 的 bug）：`auto` 时 `detectFormat` 探测，`json/yaml/toml/csv` 各走真实序列化；core 新增 `astToFormat`/`downloadMeta`，自研最小 TOML 序列化（`toml` 库无 stringify）+ 轻量 CSV 序列化。
3. **错误/提示样式暗色适配**：`.toast-float`、`.cv-toast` 背景由写死暗色改为 `var(--bg-3)`，随 `data-theme` 切换亮/暗。
4. **列视图搜索框暗色样式**：`.column-search` 此前无 CSS（浏览器默认白底），补齐为 `var(--bg-3)`/`var(--fg)`/`var(--border)`/`var(--accent)` 聚焦高亮。
5. **对比/主错误卡片「语法有误」英文适配**：`errorHumanizer.ts` 的 title/hint 原为中文硬编码，本轮改为走 i18n（`err.*` key，zh/en 补齐，CompareView/App 透传 `t`）。

你提到的两项中，**全站中英文补充已完成**（详见下方「一、2」）；**CLI 完善** 也已在本轮收尾（详见下方「一、1」），包括真实 TOML 导出、补全子命令、stdin 支持、单测与 bin 可用修复。此外还有几个值得补强的点。

**本轮（2026-08-13 续）再追加完成的 2 项**：
- **A. CLI `serve` 子命令闭环**：`jsona serve [file]` 启动本地 HTTP 服务（默认 `127.0.0.1:8080`，可 `-p`/`--host`/`--no-open`/`--no-watch`），复用离线 HTML 模板并经由 SSE（`/__jsona_live`）实现编辑即刷新；解析/读取失败返回友好错误页而非崩溃；stdin 经 `-` 输入。CLI 功能现已完全闭环（仅剩 npm publish 真机发布一项非阻塞可选项）。
- **B. 非 JSON 错误定位补全**：YAML/TOML/CSV 解析错误现携带行号并在错误卡中展示对应行 snippet（YAML 真实提取行号、CSV 走 papaparse `error.row`）；TOML 因 iarna/toml 不暴露行号退化为全文 snippet。已补 core 单测（29/29 通过）。
- **C. 大文件 Worker 取消/超时加固（部分完成）**：`parseWorker.ts` 引入 `activeId` 竞态防护——新请求/显式 `cancel` 取代在途任务，旧结果被丢弃不再覆盖新文档；`useParseWorker.ts` 新增 `latestId` 主线程校验 + 60s 超时兜底。修复了「旧大文件解析完成后错误覆盖新文档」的竞态 bug。仍未做：>50MB 内存峰值、CSV 流式、Graph 聚合展开（已在文档标记）。

---

## 一、你明确的两项

### 1. CLI 完善（已完成 ✅，本轮收尾）
此前文档自陈的 CLI 缺口，经本轮核验**大部分已存在并已补齐**：
- **TOML 导出已真实实现**：`index.ts` 的 `tomlStringify()` 自研最小序列化器（支持表 / `[[数组表]]` / 标量 / 嵌套），非假实现；`serialize()` 对未知格式回退 JSON（防御性，不再输出 `undefined`）。
- **`web` 命令已是真实离线 HTML**：自包含（无 CDN、无上传），内嵌树视图 + 搜索过滤 + 源码面板，符合「本地生成结构文档」承诺。
- **`serve` 子命令已新增（本轮回填）**：`jsona serve [file]` 启动本地 HTTP 服务（默认 `127.0.0.1:8080`，可 `-p` 改端口、`--host` 绑地址、`--no-open` 不自动开浏览器、`--no-watch` 关文件监听），复用离线 HTML 模板并通过 SSE（`/__jsona_live`）实现保存即自动刷新；解析/读取失败返回友好错误页而非崩溃进程；stdin 经 `-` 输入。本地优先、无上传承诺。
- **塑形/批量子命令齐全**：`format` / `minify` / `sort`（递归键排序）/ `diff`（复用 `diffTrees`，支持 text|json 输出）/ `inspect`（格式·类型·键数·节点数·字节数）/ `query`（jq 风格路径 + `-o` 转换）/ `web` / `serve`。
- **stdin 支持**：本轮修复 `readInput`，显式 `-` 或空参数均从 stdin 读取（`cat data.json | jsona format -`）。
- **单测 + bin 可用修复**：新增 `test/cli.test.ts`（12 例端到端，覆盖各子命令与 stdin，含 `serve` 启动→请求→关闭的生命周期测试），`package.json` 加 `test` 脚本；修复 `@jsona/core` 的 `exports`（`development`→`src`、`import`→`dist`），使编译后的 `dist/index.js` 能经 Node 正常运行，`bin` 全局可用。
- **README 已覆盖**：`packages/cli/README.md` 含全部子命令与示例（含 `serve`）。

**结论**：CLI 功能已完全闭环（含 `serve`）。剩余仅「`npm publish` 真机发布实测」一项非阻塞可选项——需在 npm 账号就绪后执行 `pnpm --filter jsona-view publish`（文档已标注），属发布流程而非代码缺口。

### 2. 全站中英文补充（已完成 ✅）
`i18n.tsx`（`translate()` + React `I18nProvider`/`useT()`）与 zh/en 双词典已健全，**全站 UI 文本均已走 `t()`**。经验证扫描（2026-08-13）：除 `i18n.tsx` 字典定义本身与两处注释外，`packages/web/src` 下所有视图（App / CompareView / ShareView / FlowView / GraphView / ColumnView / SearchResults / ErrorCard / Breadcrumb 等）**无任何硬编码中文 UI 文本**；zh/en key 已对齐（含 compare 全维度、share 全状态、flow/graph、column、search、error、err.* 语法错误文案）。
- 切换语言后全站（含错误提示、toast、空态、列视图）正确本地化，无残留中文。
- 本轮补充了 `errorHumanizer` 的 `err.*` 中英文，闭合了「语法有误」提示的英文缺口。

**结论**：此项不再是遗留项，已从「待办」移除。

---

## 二、我评估后建议补充的后续开发项

### A. 优先（体验/信任相关，工作量中等）
1. **非 JSON 格式的行号/错误定位补全（已完成 ✅，2026-08-13）**：`errorHumanizer` 对 YAML/TOML/CSV 报错仍只有笼统提示（计划 P8 仅补了「高亮偏移」，错误定位仍是 JSON 优先）。开发者用 YAML 出错时体验打折。
   - 本轮增强：`packages/core` 的 yaml/toml/csv parser 在抛 `JsonaParseError` 时提取行号（YAML 走 `yaml` 库 `linePos`、TOML 从 message `line N`、CSV 走 papaparse `error.row`）并写入 `line` 字段；`errorHumanizer` 的 YAML/TOML/CSV 分支改用统一 `line` 提取并在错误卡中展示对应行 snippet + 行号。已为三 parser 补 `line` 提取单测（core 29/29 通过）。
   - 已知边界：iarna/toml 库报错 message 不携带行号（仅报 "end of input found"），故 TOML 错误卡退化为展示全文 snippet 而不高亮特定行；这是库限制，非代码缺口。
2. **大文件性能加固（计划 P4 主体未全做）**：计划明确「>10k 节点交互流畅、diff 分页」已实现，但**解析层 Web Worker 仍是首遍进度**，超大文件（>50MB）的解析内存峰值、CSV 流式、Worker 取消/超时尚未打磨；Graph 采样阈值（>500/>2000）提示已做但无「聚合节点展开」。
   - **本轮（2026-08-13）已完成的 Worker 取消/超时部分**：`parseWorker.ts` 引入 `activeId` 校验——新请求（或显式 `cancel`）取代在途任务，旧任务在下次 tick / parse 前后均被丢弃，不再覆盖新文档结果（修复竞态 bug：旧大文件解析完成会错误 setState 覆盖新文档）；`useParseWorker.ts` 新增主线程 `latestId` 校验丢弃过期 inbound 消息 + `PARSE_TIMEOUT_MS=60_000` 超时兜底（超时显示「解析超时」而非永久转圈）。
   - **仍未做（明确标记）**：超大文件（>50MB）解析内存峰值控制、CSV 流式解析、Graph 聚合节点「展开」交互。这些需单独评估，未在本轮覆盖。
3. **工作区同步的冲突/版本**：~~`useWorkspace` 仅存/取/删，计划承认「未做冲突合并/版本历史」，多设备编辑会静默覆盖。~~ **已完成（2026-08-13）**：服务端 `version` 字段 + `PUT` 乐观并发 409 冲突 + `WorkspaceSync` 冲突裁决 UI，多设备编辑不再静默覆盖（见「工作区冲突合并」小节）。

### B. 增值（商业化闭环，工作量较大）
4. **OAuth 真机走查**：计划 P7 末尾自陈「GitHub App 回调需公网可达，本环境未实测」——这是收费档「工作区同步」可信度的关键，需一次公网实测。
5. **AI 处理（计划 V2/P6）**：~~`topbar.ai` 按钮当前是「即将上线」占位，未接任何后端/配额。~~ **已完成（2026-08-13）**：见「AI 三层架构（L1 本地 / L2 云端 LLM / L3 MCP）」小节——本地分析免费离线、云端端点 + 配额已接，且 L2 已落地 **BYOK 真实转发**（用户填自己的 OpenAI/Anthropic/兼容网关 key，后端零成本代理，不托管模型、不收 token 费）；jsona 作为 MCP Server 已可在 CLI 运行（`jsona mcp`，官方 SDK 标准化，迁移到开源 CLI 包）。
6. **分享链接付费档位**：~~服务端已有 TTL/purge，但「免费 7 天 / Pro 30 天 / Team 90 天+密码」的**配额与鉴权**未接（当前所有分享等长 TTL）。~~ **已完成（2026-08-13）**：见「分享付费档（配额与鉴权）」小节，三档配额已接 `POST /api/share` 鉴权。

### C. 工程化（降低维护风险）
7. **E2E 回归固化**：计划用 Chrome MCP 走查（16/16 通过）但未沉淀为可重跑脚本；核心路径（分享 hash、双向定位、CLI 往返）建议固化为 Playwright/脚本防回归。
8. **`server` 与 `web` 的 CORS/部署文档**虽已有，但 **OAuth + 工作区端到端联调文档**缺。
9. **CLI 的 `bin` 与发包**：`package.json` 是否配 `bin`、能否全局 `jsona` 未验证。

---

## 我的优先级建议

| 优先级 | 项 | 状态 | 理由 |
|---|---|---|---|
| ✅ 已完成 | 全站中英文补全 | 已完成（2026-08-13 扫描确认零残留） | 半吊子 i18n 比没有更糟，已点名并闭环 |
| ✅ 已完成 | CLI 真实导出/子命令/serve | 已完成（2026-08-13） | 计划承诺 + MIT 开源获客核心，TOML 真实导出、`web`/`serve` 离线查看、单测 12/12、bin 可运行 |
| ✅ 已完成 | 非 JSON 错误定位 | 已完成（2026-08-13） | YAML/TOML/CSV 解析错误现携带行号并展示对应行 snippet，core 单测 29/29 |
| 🟡 代码已完成 / ⏳ 公网实测待做 | OAuth 公网实测 | 阻塞：需真实 GitHub App 凭据 + 公网回调域名 | 收费档可信度前提；`server/src/oauth.ts` 已完整实现 authorize→callback→token 交换→用户校验→session cookie→me/logout，`server.ts` 在 `oauthEnabled` 时挂载并限流。本环境无真实 `GITHUB_CLIENT_ID/SECRET` 且 GitHub OAuth 要求回调公网可达，无法本地真机走查。**本地可验证项**：未配置凭据时 `/api/oauth/login` 返回 501、`/api/health` 显示 `oauth:false`；此路径可本机冒烟。真机走查待部署到公网 + 配置凭据后执行。 |
| 🟡 部分完成 | 大文件解析峰值/取消 | 进行中 | 计划 P4 余量 |
| P2 | AI 占位接后端 | 待办 | V2 规划，但需后端投入 |
| P3 | 工作区冲突合并、分享付费档 | 🟢 均已完成 | 冲突合并见「工作区冲突合并」小节；付费档配额与鉴权见「分享付费档（配额与鉴权）」小节 |

**下一步执行顺序（已与你确认）**：全站 i18n 已收尾 → **开工 CLI 真实能力**（含真实 TOML 导出 + `format`/`minify`/`sort`/`diff` 子命令 + `web` 升级离线 HTML + 单测 + README + `bin`）。你已确认「好了，继续」，我现在从 CLI 开始。

---

## 收尾与发布 Checklist（2026-08-13 盘点）

全功能已落地且 `core/web/cli/server` 均 typecheck + build 通过。进入收尾阶段，按"必须决策 → 文档补全 → 发布动作"三级推进。

### A. 必须你拍板的事项（有阻塞风险）

1. **许可证冲突（发布前必解）**：`web` 是 **GPL-3.0-or-later**，而 `core`/`cli` 与根仓库是 **MIT**。GPL 与 MIT 不兼容，且根 `package.json` 统写 MIT 会误导。两条路二选一：
   - **(推荐)** 全仓统一为 MIT（最简单，cli/core 已 MIT，只需把 `web` 的 `license` 改成 MIT，并在 web 的第三方依赖许可（Monaco、react-force-graph、three 等）上复核是否允许 MIT 再分发）。
   - 或接受"web 是 GPL、cli/core 是 MIT"的双许可结构，但根 `package.json` 必须改为 `UNLICENSED`/明确多许可，并在 README 写清"web 子包 GPL，其余 MIT"，否则 `npm publish` 时许可证元数据自相矛盾。
2. **是否真要发布**：`cli` 的 `bin`/`files` 已就绪，npm 包名定为 **`jsona-view`**（`jsona` 已被占用，`@jsona/cli` 也被占用），core 依赖包名为 **`jsona-core`**（已发布）。确认版本号（首版 `0.1.0`）与是否公开（`npm publish --access public`）。

### B. 你需要准备的环境 / 凭证（仅当启用对应功能）

| 用途 | 变量 | 在哪里 | 不配会怎样 |
|---|---|---|---|
| 分享服务器会话签名 | `SESSION_SECRET` | `server/.env.example` | 启动报错 |
| GitHub 登录（workspace 同步 + AI 设置按钮可见） | `GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET` | 同左 | web 无登录入口，AI 设置按钮不出现 |
| AI 密钥加密主密钥 | `BYOK_MASTER_KEY`（≥32 字节 hex/base64） | `server/.env` | 退化为进程内随机密钥，**重启后旧 AI key 不可解密**（仅 obscuration） |
| 前端连分享服务器 | `VITE_API_BASE`（如 `http://localhost:8787`） | `packages/web/.env` | web 纯离线，无登录/分享/AI 设置 |
| CORS 白名单 | `PUBLIC_ORIGINS`（逗号分隔或 `*`） | `server/.env` | 跨域请求被拒 |

> 本地纯离线体验（打开网页即可用）**不需要任何凭证**；只有"分享短链 / GitHub 登录 / BYOK AI"才需要后端 + 上述变量。

### C. 文档补全（无阻塞，建议收尾做）

- [x] `server/README.md` 已存在（含存储/限流/配置/部署）。
- [ ] **根仓库 README.md（缺失）**：建议补"是什么 / 三大包 / 5 秒上手 / 架构图 / 许可证"；本仓库目前只有 `server/README.md`。
- [ ] **`web` 部署说明**：`VITE_API_BASE` 注入方式、`vite build` 产物、`base` 路径（若挂子路径需 `--base=/jsona/`）。
- [ ] `server/README.md` 首行 "(closed source)" 与公开仓库矛盾——改为说明"本目录是可选的后端，前端可纯离线运行"，删除 closed source 措辞。
- [x] 本文件 GraphView 章节已按 **react-force-graph** 真实实现修正（早期选型表曾写"自研 SVG 力导向"，实际 2026-08-08 已重写为 react-force-graph-2d/3d）。

### D. 发布动作（确认 A 后执行）

1. `pnpm -r build` 全量构建（core → web/cli → server）。
2. `cli`：`cd packages/cli && npm publish --access public`（先 `npm view jsona` 查重改名）。
3. `web`：静态托管 `packages/web/dist`（GitHub Pages / Vercel / 任意静态服务）；若连分享服务器，部署后置 `VITE_API_BASE` 为线上地址。
4. `server`：按 `server/README.md` 的 Docker / node 部署，配齐 `.env`。
5. 发布前再跑一次 `pnpm test`（core 单测 23/23 + cli 单测）做回归门槛。

#### D.0 ⚠️ npm 发布 2FA 认证方式（2026-08-15 实测，必读）

账号 `andrewlaredo` 的 2FA 绑定的是**设备 PIN / 通行密钥（passkey / WebAuthn）**，
**不是** TOTP 验证器（无 6 位码）。发布时踩过的坑与正确流程：

- ❌ `--otp <6位码>` 无效：该类账号根本不产生 TOTP 码。
- ❌ granular token 的 "bypass 2FA" 选项**不生效**：用 token 发布稳定返回
  `403 ... Two-factor authentication or granular access token with bypass 2fa enabled is required`。
- ✅ **正确流程**：直接执行 `npm publish --access public`，终端会打印
  `https://www.npmjs.com/auth/cli/...` 链接，**手动在浏览器打开完成一次设备验证**，
  认证成功后发布自动继续并完成。

发补丁 / 新版本：改对应包 `package.json` 的 `version` → 重新 `build` → 重复 publish + 浏览器链接认证。
完整步骤见 `DEPLOY.md` 第一节。

> 2026-08-15 已实际发布：`jsona-core@0.1.0`、`jsona-view@0.1.0` 均成功上 npm。

### E. 已知限制（发布说明里应写清）

- MCP server 的 roots 仅进程内 TTL 缓存（SDK 1.30 未暴露 `notifications/roots/list_changed` 订阅钩子）。
- OAuth `authorize` 为自动批准（headless，无人工 consent 页，生产建议改手动确认）。
- AI 为 BYOK：jsona 不托管模型、不收 token 费；整文档发送开关未做（仅发结构摘要）。
- 许可证：见 A.1。
{
  "name": "@jsona/share-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "jsona share-link + workspace-sync backend (closed source)",
  "license": "UNLICENSED",
  "scripts": {
    "start": "node --env-file=.env dist/server.js",
    "dev": "node --env-file=.env --watch src/server.js",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "express": "^4.22.2",
    "nanoid": "^5.1.16",
    "node-sqlite3-wasm": "^0.8.60"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.19.43",
    "typescript": "^5.5.0"
  }
}
